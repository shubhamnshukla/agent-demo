[project]
name = "agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "definable[serve]>=0.8.0",
    "httpx[http2]>=0.28.1",
    "numpy>=2.4.4",
]
